---
import Layout from "../../layouts/Layout.astro";
import { t } from "../../i18n";

import LinkBox from "../../components/about/LinkBox.astro";
import MarqueeText from "../../components/about/MarqueeText.astro";
import PhysicsPile from "../../components/about/PhysicsPile.astro";

export async function getStaticPaths() {
  return [{ params: { locale: "en" } }, { params: { locale: "de" } }];
}

const { locale } = Astro.params;
---

<Layout>
  <div class="pt-[70px]">
    <div class="container mx-auto px-8">
      <h1 class="flex flex-col text-4xl mb-12 max-w-[250px] w-full">
        <span class="font-balete-black self-start">Eine Kunst</span>
        <span class="font-balete-black self-end">ausstellung</span>
        <span class="font-balete-italic self-center">über das</span>
        <span class="font-balete-italic self-start">Dazwischen-Sein.</span>
      </h1>
      <p class="mb-4">
        Es gibt Orte, die nicht dafür gedacht sind, dass man bleibt. Orte ohne Ankunft, ohne Ziel, Schwellenräume, sogenannte liminal spaces.
      </p>
      <p class="mb-4">
        Liminalität beschreibt den Zustand zwischen zwei Zuständen, Orten oder Lebensphasen, wie ein Korridor zwischen zwei Räumen, ein Bahnhof um Mitternacht, oder der flüchtige Moment zwischen Kindheit und Erwachsensein.
      </p>
    </div>
    <LinkBox />
    <div class="container mx-auto px-6">
      <p class="mb-4">
        Diese Ausstellung ist kein Ziel. Sie ist ein Moment. Ein Innehalten. Ein Übergang, der - vielleicht - in Hingabe endet.
      </p>
      <p class="mb-4">
        AM I REAL? fragt nicht: Wer bist du? <br> Sondern:
      </p>
    </div>
    <MarqueeText />
    <PhysicsPile />
  </div>
</Layout>


 class="font-balete-black text-4xl">{t(locale, "about.title")}